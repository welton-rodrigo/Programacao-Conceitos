//the arrays and objects can also contain other arrays and objects

var Data = {
    trial1: [[1,2],[3,4]]
}

console.log(Data)

/**funçoes é um bloco de código javaScritp nomeado e parametrizado
que voce define uma vez, e então pode chamar repetidas vezes**/
function somaNumero(x){
    return x + 1;
}
//Interessante funções são valores e podem ser atribuidas a variaveis
var square = function somaTest(d){
    return d + 2;
};